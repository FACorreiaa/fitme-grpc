exporters:
  otlp:
    endpoint: "localhost:4317"  # Replace with your Grafana Alloy host
    insecure: true  # Use insecure connection

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
    metrics:
      receivers: [otlp]
      exporters: [otlp]
